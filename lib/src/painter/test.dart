import 'dart:convert';
import 'dart:io';

void main() {
  // stdout.write(utf8.decode([27, 107, 126, 27, 92]));
  // String a = stdin.readLineSync();
  print('� � \uFFFD'.codeUnits);
  // print('\x1b'.codeUnits);
}
